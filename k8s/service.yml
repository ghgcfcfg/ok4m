apiVersion: v1
kind: Service
metadata:
  name: asdjk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "asdjk"
    port: 8080
  selector:
    app: asdjk
